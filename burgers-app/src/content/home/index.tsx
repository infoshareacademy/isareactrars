import { PageWrapper } from "../../common/page-wrapper";
import { Typography } from "@mui/material";

export const Home = () => {
    return (
        <PageWrapper title="Hello">
            <Typography variant="h5">RARS burgers</Typography>
        </PageWrapper>
    )
}